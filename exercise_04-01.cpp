int main()
{
  // define words and hints
  // choose a word and hint
  // scramble the chosen word

  // print welcome screen
  // ask for a guess

  // while the guess is wrong, or the user doesn't quit
  //   if requested, print the hint, otherwise print "not guessed" message
  //   ask for a guess

  // if the player guessed the word, print a message
  // print exit screen

  return 0;
}